# プロジェクトの問題点と改善案

このプロジェクトに関する、既知の問題点や将来的な改善案を以下にまとめます。

## 1. APIキーの管理方法

### 現状
- DeepL APIの認証キーは、PCの**環境変数** (`DEEPL_SCRIPT_TRANSLATION`) に設定するか、マクロファイル (`HmDeepLLangTranslator.mac`) 内に**直接書き込む**必要があります。

### 問題点
- **環境変数の設定は、ITに詳しくない利用者にとってはハードルが高い**可能性があります。
- マクロファイルへの直接書き込みは、キーが外部に漏洩するリスクがあり、セキュリティ上好ましくありません。また、マクロの更新時に再設定が必要になり、手間がかかります。

### 改善案
- `.env` ファイルや、専用の設定ファイル（例: `config.ini`）をマクロと同じフォルダに配置し、そこからキーを読み込む仕組みを導入する。これにより、利用者にとって設定が簡単になり、セキュリティも向上します。

## 2. エラーハンドリング

### 現状
- Python/Node.jsスクリプト内で発生した翻訳エラーは、秀丸エディタのアウトプット枠に表示されます。

### 問題点
- `python.exe` や `node.exe` の**実行ファイル自体が見つからない場合**（PATHが通っていないなど）や、必要なライブラリ（`deepl`, `deepl-node`）が**インストールされていない場合**のエラーが、利用者にとって分かりにくい形でしか通知されません。結果として、マクロを実行しても何も起こらないように見えてしまいます。

### 改善案
- メインマクロ (`HmDeepLLangTranslator.mac`) 側で、`runProcess` の戻り値や標準エラー出力をより詳細にチェックし、「Pythonが見つかりません」や「`deepl`ライブラリをインストールしてください」といった、具体的なエラーメッセージをダイアログ等で表示する処理を追加する。

## 3. 翻訳言語の追加・設定方法

### 現状
- 翻訳の方向（例: 日本語→英語）は、`HmDeepLLangTranslator2En.mac` のように、専用のマクロファイルによって決まっています。
- 新しい言語の組み合わせ（例: 日本語→ドイツ語）を追加したい場合、既存の呼び出し用マクロをコピーし、引数を書き換えた新しいマクロファイルを作成する必要があります。

### 問題点
- 対応言語を増やしたいだけなのに、ファイルの数が増えてしまい、管理が煩雑になります。

### 改善案
- マクロ実行時に、翻訳先の言語を選択できるようなダイアログを表示する機能を設ける。
- もしくは、設定ファイルに対応言語のリストを記述できるようにし、マクロがそれを読み込んで動的にメニューを生成する仕組みを検討する。
